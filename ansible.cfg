[defaults]
# host_key_check disabled to avoid issues connecting to the Servers *Is a security risk*
host_key_checking = False

# Make user we are using local inventory and not the default one
inventory=./inventory

log_path= /home/bdollerup/Ansible/logs/logfile.log
roles_path= ./roles 
nocows=true
callback_whitelist = profile_tasks

